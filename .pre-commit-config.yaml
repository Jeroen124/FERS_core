repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: 'v0.6.7'
    hooks:
      # Run the linter.
      - id: ruff
        pass_filenames: false
        args: [ --fix ]
      # Run the formatter.
      - id: ruff-format